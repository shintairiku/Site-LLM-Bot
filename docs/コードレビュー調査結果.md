# AIチャットボット v2.0 コードレビュー調査結果

調査日: 2026-05-12

最終更新: 2026-05-15

## 調査目的

`AI_Chatbot_仕様書_v2.0.md` を前提に、現在のリポジトリ構成と実装状況を確認し、販売先へ以下を配布する想定でコードレビューを行った。

- スクリプトタグ
- ウィジェット JavaScript
- ウィジェット CSS

## 仕様書から読み取ったプロジェクト概要

本プロジェクトは、住宅・リフォーム業向けホームページに埋め込む AI チャットボットである。DB や事前インデックスを持たず、リクエスト時にテナント HP 限定の Web Search を行い、その結果を根拠として LLM が回答するシンプルな構成を目指している。

主な要件は以下。

- テナント HTML に 1 行の script タグで設置する
- 右下固定ボタンからチャット画面を開く
- PC は 380 x 600px 程度、SP はフルスクリーン表示
- LLM 応答は SSE ストリーミングで逐次表示する
- API は `/v1/chat/message`、`/v1/chat/session`、`/v1/widget/config` を提供する
- API は API キーまたは公開トークン相当の認証と Origin 検証を行う
- CORS はテナント登録ドメインのみ許可し、ワイルドカードは禁止する
- 会話履歴は初期段階ではオンメモリ管理、TTL は 30 分
- 検索対象はテナントごとの `allowed_domains` に限定する

## 現在のリポジトリ構成

主要ファイルは以下。

| パス | 役割 |
|---|---|
| `AI_Chatbot_仕様書_v2.0.md` | 現行仕様書 |
| `site_llm_bot/api/app.py` | FastAPI アプリ本体 |
| `site_llm_bot/services/openai_handler.py` | OpenAI Responses API 呼び出し |
| `site_llm_bot/services/session_store.py` | オンメモリセッション管理 |
| `site_llm_bot/config.py` | 環境変数・テナント設定読み込み |
| `config/tenants.json` | サンプルテナント設定 |
| `static/widget.js` | ローカル / Cloud Run 用の正式配布ウィジェット JS |
| `static/widget.css` | ローカル / Cloud Run 用の正式配布ウィジェット CSS |
| `static/mock-widget.js` | ローカル / Cloud Run 用ウィジェット JS |
| `static/mock-widget.css` | ローカル / Cloud Run 用ウィジェット CSS |
| `public/static/widget.js` | Vercel 静的配信用の正式配布 JS |
| `public/static/widget.css` | Vercel 静的配信用の正式配布 CSS |
| `public/static/mock-widget.js` | Vercel 静的配信用 JS |
| `public/static/mock-widget.css` | Vercel 静的配信用 CSS |
| `demo/sample_page.html` | デモページ |
| `tests/test_chat_api.py` | API / OpenAI ハンドラのテスト |
| `docs/サーバー設計.md` | 販売前提のサーバー設計メモ |
| `docs/デプロイ.md` | Vercel / Cloud Run デプロイ手順 |

## 現在実装できていること

- FastAPI アプリの起動口がある
- `/api/chat` でチャットリクエストを受け付ける
- OpenAI Responses API の `web_search` ツールを使う payload を構築している
- テナントごとの `allowed_domains` を OpenAI payload に渡している
- OpenAI API キー未設定時はデモ応答にフォールバックする
- オンメモリでセッション ID と履歴を管理している
- セッションはテナントに紐づき、別テナントからの再利用を拒否する
- `/v1/widget/config`、`/v1/chat/session`、`/v1/chat/message` を提供している
- `/v1/chat/stream` で SSE ストリーミング応答を返している
- 右下固定ボタン型のウィジェット UI がある
- 正式配布名として `widget.js` / `widget.css` を追加済み
- JS / CSS は仕様の 50KB gzip 以内に収まっている
- `.venv/bin/pytest -q` では 27 件のテストが通過した

## 対応状況

| 項目 | 状態 | ブランチ | 主なコミット | メモ |
|---|---|---|---|---|
| 認証・Origin 検証 | 対応済み | `fix/auth-origin-guard` | `39b1dc8` | `X-Tenant-Id`、`X-Widget-Token`、`Origin` を検証。CORS ワイルドカードを廃止 |
| dev 環境 Origin 許可 | 対応済み | `fix/auth-origin-guard` | `682e759` | `site-llm-bot-dev.vercel.app` と Cloud Run dev Origin を許可 |
| 古い本番 JS 固定参照 | 対応済み | `fix/auth-origin-guard` | `5800ae5` | デモページのウィジェット参照を `/static/mock-widget.js` に変更 |
| `/v1` API 契約 | 対応済み | `feat/v1-api-contract` | `b71cf19` | 設定取得、セッション開始、メッセージ送信 API を追加 |
| 配布用ウィジェット整理 | 対応済み | `chore/widget-distribution` | `599cc3d` | `widget.js/css` を追加し、デモページも正式 JS 参照へ変更 |
| セッションのテナント紐づけ | 対応済み | `fix/session-tenant-scope-v2` | `adb9288` | セッションにテナントを紐づけ、別テナント再利用を拒否 |
| SSE ストリーミング | 対応済み | `feat/sse-streaming-v2` | `7dab222` | `/v1/chat/stream` とウィジェット側ストリーム受信を追加 |
| レートリミット | 未対応 | `feat/rate-limit` | - | 認証・セッション後に対応 |
| テナント別 CSS | 対応済み | `feat/tenant-specific-css` | - | `data-color` を廃止し、`tenants/{tenant_id}.css` を追加読み込み |
| README / 運用手順整理 | 未対応 | `docs/runtime-readme` | - | 起動、テスト、配布タグ例の整備が必要 |

## 2026-05-15 再分析で確認した修正対象

現在のリポジトリを再確認した結果、認証・Origin 検証、`/v1` API、セッションのテナント紐づけ、SSE ストリーミング、正式配布名の `widget.js/css` は実装済みである。一方、販売前の配布物としては以下の修正が必要である。

| 優先度 | 修正対象 | 現状 | 必要な対応 | 主な対象ファイル |
|---|---|---|---|---|
| P1 | レートリミットと最低限のアクセスログ | `/v1/chat/session`、`/v1/chat/message`、`/v1/chat/stream`、`/api/chat` が無制限。アクセスログも構造化されていない | プロセス内メモリで `tenant_id + client_ip` 単位の短時間制限を追加し、429 応答とテストを追加する。併せて `tenant_id`、`origin`、`ip`、`path`、`status_code`、`latency_ms` をログ出力する | `site_llm_bot/api/app.py`、`tests/test_chat_api.py` |
| P1 | テナント別 CSS 適用 | `static/widget.js` と `public/static/widget.js` は `data-color` と JS 内の CSS 変数更新で色を変える設計だった。顧客側が CSS / API 操作を行える前提になっている | 顧客側の `data-color` 指定を廃止し、共通 `widget.css` の後に `tenants/{tenant_id}.css` を自動読み込みする。顧客要望に応じた見た目調整は、こちら側で tenant 別 CSS を編集して対応する | `static/widget.js`、`public/static/widget.js`、`static/tenants/*.css`、`public/static/tenants/*.css`、`demo/sample_page.html`、`tests/test_chat_api.py` |
| P2 | ウィジェットの `/v1/widget/config` テキスト連携 | `static/widget.js` と `public/static/widget.js` は `data-tenant-name`、JS 内の固定 greeting / suggested questions を表示に使っている。設定取得 API はサーバー側にあるがウィジェットから未使用 | 初期化時に `/v1/widget/config` を取得し、表示名、挨拶、候補質問などのテキスト系公開設定だけを反映する。テーマカラーなど CSS 操作は API から外す | `static/widget.js`、`public/static/widget.js`、`demo/sample_page.html`、`tests/test_chat_api.py` |
| P2 | 本番ウィジェットのモック応答フォールバック整理 | API 失敗時に常にモック応答を表示するため、本番障害や認証設定ミスが利用者に見えにくい | モック応答はデモ用途に限定し、本番では接続失敗メッセージを表示する。必要なら `data-demo-mode` のような明示フラグで切り替える | `static/widget.js`、`public/static/widget.js`、`demo/sample_page.html` |
| P2 | README / 運用手順 | `README.md` がタイトルのみで、ローカル起動、`.venv/bin/pytest -q`、環境変数、テナント設定、配布 script タグ例が未記載 | 開発者が再現できる最小手順と、販売先へ渡す script タグ例、Vercel / Cloud Run 運用時の注意点を記載する | `README.md`、`docs/デプロイ.md` |
| P2 | SSE API 契約差分の整理 | 仕様書は `POST /v1/chat/message` の SSE 返却を想定しているが、現実装は JSON の `/v1/chat/message` と SSE の `/v1/chat/stream` が併存している | 販売前の API 契約として、`/v1/chat/stream` を正式化して仕様書へ反映するか、`/v1/chat/message` へ SSE を統一するかを決める | `AI_Chatbot_仕様書_v2.0.md`、`docs/サーバー設計.md`、`site_llm_bot/api/app.py`、`static/widget.js` |

今回の CSS 仕様変更は `feat/tenant-specific-css` で対応する。レートリミット、README、SSE API 契約整理は別作業として残す。

## 重大度別レビュー

### High: 認証・Origin 検証が未実装

仕様では、ウィジェットと API の間で API キーまたは公開トークン相当の認証と Origin 検証が必要である。しかし現在は `tenant_id` をリクエストボディで渡すだけで `/api/chat` を呼び出せる。

さらに `CORSMiddleware` が `allow_origins=["*"]` になっており、任意 Origin から呼び出し可能である。

影響:

- 第三者が API を直接呼び出せる
- LLM API コストが不正利用される
- テナントなりすましが可能になる
- 仕様の「ワイルドカード不可」に反する

該当箇所:

- `site_llm_bot/api/app.py`
- `config/tenants.json`

推奨対応:

- `TenantConfig` に `allowed_origins`、`allowed_origin_patterns`、`public_token`、`status` を追加する
- `X-Tenant-Id` と `X-Widget-Token` を検証する依存関数を追加する
- `Origin` がテナント設定に含まれる場合のみ許可する
- 本番環境では `null` Origin を拒否する
- CORS のワイルドカード許可を廃止する

対応状況:

- `fix/auth-origin-guard` で対応済み
- CORS はテナント設定の `allowed_origins` / `allowed_origin_patterns` から構築する形へ変更
- 実リクエストでは `X-Tenant-Id`、`X-Widget-Token`、`Origin` を検証
- Vercel dev / Cloud Run dev の動作確認で不足していた Origin を追加
- デモページが古い本番 Cloud Run の JS を固定参照していたため、`/static/mock-widget.js` 参照へ修正

### High: 仕様上の `/v1` API が未実装

仕様書では以下の API が定義されている。

- `POST /v1/chat/message`
- `POST /v1/chat/session`
- `GET /v1/widget/config`

現状は `/api/chat` のみで、レスポンスも JSON 一括返却である。

影響:

- 販売用ウィジェットと API 契約が固まっていない
- セッション開始、設定取得、メッセージ送信の責務が分離されていない
- 本番用 API に移行する際にウィジェット側の破壊的変更が必要になる

推奨対応:

- `/api/chat` はデモ互換として残し、本番用に `/v1/*` を追加する
- `/v1/widget/config` で表示名、挨拶、候補質問などのテキスト系公開設定を返す
- `/v1/chat/session` でセッションを明示発行する
- `/v1/chat/message` にチャット送信処理を移す

対応状況:

- `feat/v1-api-contract` で対応済み
- `GET /v1/widget/config` でテナント表示名、挨拶、候補質問を返す
- `POST /v1/chat/session` でセッション ID と有効期限を返す
- `POST /v1/chat/message` は JSON 応答の互換 API として実装済み
- 既存 `/api/chat` はデモ互換用として残置

### High: SSE ストリーミング未実装

仕様では LLM 応答を SSE で逐次返却し、ウィジェット側でタイピング表示する想定である。現状は FastAPI 側が通常 JSON を返し、ウィジェット側も `fetch()` で完了応答を待つ。

影響:

- 初回表示までの体感速度が悪くなる
- 仕様の SSE レスポンスイベント `chunk` / `done` / `error` に対応できない
- LLM 応答中の UX が弱い

推奨対応:

- FastAPI 側に `StreamingResponse` または SSE 用レスポンスを追加する
- OpenAI Responses API の streaming に対応したハンドラを追加する
- ウィジェット側で `ReadableStream` または `EventSource` 相当の処理を実装する
- イベント形式を `chunk`、`done`、`error` に統一する

対応状況:

- `feat/sse-streaming-v2` で対応済み
- FastAPI 側に `StreamingResponse` を追加
- OpenAI Responses API の streaming を読む `generate_answer_stream` を追加
- ウィジェット側は `ReadableStream` で `chunk` / `done` / `error` を処理
- 現状の SSE エンドポイントは `/v1/chat/stream`

補足:

- 仕様書上は `POST /v1/chat/message` が SSE 対象だが、現実装では JSON 互換の `/v1/chat/message` と SSE 用の `/v1/chat/stream` が併存している。販売前に API 契約としてどちらに統一するか確認が必要。

### High: 配布用ウィジェットとして未整理

現在の配布対象は `mock-widget.js` / `mock-widget.css` という名前であり、Cloud Run の URL がハードコードされている。候補質問、挨拶、テナント名も JS 内に初期値として入っている。

販売先へ配る前提では、静的アセットは共通化し、テナント差分は script タグの `data-*` と API 設定取得で吸収する必要がある。

影響:

- テナント追加時に JS を編集する運用になりやすい
- ステージング / 本番 URL の切り替えが不安定になる
- 販売先へ「mock」という名前のファイルを配布することになる

推奨対応:

- `widget.js`、`widget.css` へ名称を整理する
- script タグは `data-tenant-id` と `data-public-token` のみに近づける
- `data-api-base` は開発・検証用途に限定する
- 表示名、挨拶、候補質問、テーマカラーは `/v1/widget/config` から取得する
- CDN / 静的配信向けにキャッシュ方針とバージョンパスを決める

対応状況:

- `chore/widget-distribution` で一部対応済み
- 正式配布名として `static/widget.js`、`static/widget.css`、`public/static/widget.js`、`public/static/widget.css` を追加
- CSS class prefix は `site-llm-bot-*` へ変更
- デモページは `/static/widget.js` を参照
- 既存 `mock-widget.js/css` は互換用に残置
- `/v1/widget/config` からの動的設定取得は未対応

想定 script タグ:

```html
<script
  src="https://widget.example.com/widget.v1.js"
  data-tenant-id="tenant-abc"
  data-public-token="public_xxxxx"
  defer
></script>
```

### Medium: `allowed_origins` が設定ファイルにあるが使われていない

`config/tenants.json` には `allowed_origins` と `allowed_origin_patterns` が定義されている。しかし `TenantConfig` には該当フィールドがなく、読み込み・検証に使われていない。

影響:

- 設定ファイルと実装の意味がズレている
- ドキュメント上は Origin 制御しているように見えるが実際には無効
- レビューや運用時に誤解を招く

推奨対応:

- `TenantConfig` に Origin 関連フィールドを追加する
- Origin 検証ロジックをテストする
- 現状と矛盾するテストを修正する

対応状況:

- `fix/auth-origin-guard` で対応済み
- `TenantConfig` が `allowed_origins`、`allowed_origin_patterns` を読み込むように変更
- 未登録 Origin を拒否し、登録 Origin を許可するテストを追加

### Medium: セッションがテナントに紐づいていない

現在のセッションストアは `session_id` をキーに履歴を保持している。テナント ID や Origin と紐づいていないため、同一 `session_id` を別テナント指定で送ると履歴混在の余地がある。

影響:

- テナント間で会話履歴が混ざる可能性がある
- 認証追加後にセッション整合性の検証が必要になる

推奨対応:

- `ChatSession` に `tenant_id`、`origin`、`page_url` を持たせる
- `get_or_create` 時にテナント不一致を拒否する
- `/v1/chat/session` でセッションを作成し、以降は同じテナントでのみ使えるようにする

対応状況:

- `fix/session-tenant-scope-v2` で対応済み
- `ChatSession` に `tenant_id`、`origin`、`page_url` を保持
- 既存セッションを別テナントから再利用した場合は `403 session tenant mismatch` を返す
- `/api/chat`、`/v1/chat/message`、`/v1/chat/stream` のセッション利用時にテナント整合性を検証

### Medium: レートリミットが未実装

仕様では Redis なしでも実装可能な IP / API キー単位のレートリミットが求められている。現状は無制限である。

影響:

- LLM API コストが急増する
- 悪意ある連投や誤実装に弱い
- 同時接続 30 セッション / テナントの運用目標を守りにくい

推奨対応:

- 初期実装としてプロセス内メモリで `tenant_id + ip` 単位の制限を入れる
- `/v1/chat/message` は 1 分あたり 20 回程度から開始する
- 将来 Redis へ差し替えやすい interface を用意する

### Low: テストが販売仕様ではなく現実装を固定している

現在のテストには、CORS が任意 Origin を許可することを期待するケースがある。これは販売前提の仕様と逆である。

影響:

- 仕様準拠へ修正したときにテストが失敗する
- セキュリティ上の問題をテストが肯定してしまう

推奨対応:

- 未登録 Origin を拒否するテストへ変更する
- `X-Tenant-Id`、`X-Widget-Token`、`Origin` の組み合わせ検証を追加する
- `/v1/widget/config`、`/v1/chat/session`、SSE イベント形式のテストを追加する

## 配布物観点の評価

### スクリプトタグ

現状はデモページ内で以下に近い埋め込みになっている。

```html
<script
  id="site-llm-bot-widget"
  src="/static/widget.js"
  data-tenant-id="sample-shintairiku"
  data-tenant-name="新大陸"
  data-public-token="public_sample_shintairiku"
  data-api-base="__WIDGET_API_BASE__"
></script>
```

販売用としては、`data-tenant-name` は将来的に設定取得 API へ寄せ、`data-color` は廃止する。CSS は顧客側で操作させず、こちら側で `tenants/{tenant_id}.css` を手動調整する。顧客に渡す script タグは、テナント ID と公開トークン程度に抑える。

### ウィジェット JavaScript

良い点:

- 自己実行関数でグローバル汚染を抑えている
- CSS を自動読み込みしており、script タグだけでも動く
- DOM 追加時に外部入力を text node 化しており、基本的な XSS 配慮がある
- gzip サイズは小さい

改善点:

- `mock-widget.js` から正式名称へ変える
- API URL のハードコードをなくす
- `/api/chat` ではなく `/v1/*` を呼ぶ（SSE は `/v1/chat/stream` を利用中）
- 設定取得 API は表示名、挨拶、候補質問などのテキスト設定に限定して使う
- SSE ストリーミングを処理する（対応済み）
- API 失敗時にモック応答を返す仕様は本番では無効化する

### ウィジェット CSS

良い点:

- UI は軽量で、右下固定ボタンとパネル表示に対応している
- SP ではフルスクリーンになる
- CSS サイズは小さい

改善点:

- `mock-chatbot-*` という class prefix を正式 prefix に変える
- テナントサイトの CSS と衝突しにくい命名にする
- Shadow DOM 採用も検討する
- テーマ変数は `/v1/widget/config` ではなく、こちら側で管理する tenant 別 CSS で反映する

## テスト結果

実行結果:

```bash
.venv/bin/pytest -q
```

結果:

```text
27 passed
```

補足:

通常の `pytest -q` はグローバル Python 環境に `fastapi` がなく失敗した。開発時は `.venv/bin/pytest` を使うか、環境構築手順を README に明記する必要がある。

## 推奨対応順

1. `[対応済み]` `TenantConfig` に認証・Origin 関連項目を追加する
2. `[対応済み]` `X-Tenant-Id`、`X-Widget-Token`、`Origin` の検証を実装する
3. `[対応済み]` CORS ワイルドカードを廃止する
4. `[対応済み]` `/v1/widget/config` を追加する
5. `[対応済み]` `/v1/chat/session` を追加し、セッションをテナントに紐づける
6. `[対応済み]` `/v1/chat/message` を追加する
7. `[対応済み]` SSE ストリーミングに対応する
8. `[対応済み]` `mock-widget.js/css` を `widget.js/css` として整理する
9. `[一部対応済み]` ウィジェットから `/v1/*` を呼ぶように変更する
10. レートリミットと最低限のアクセスログを追加する
11. `[対応済み]` テストを販売仕様に合わせて更新する
12. README にローカル起動、テスト、配布 script タグ例を記載する

## 結論

現在の実装は、仕様書 v2.0 の MVP 方針に沿ったプロトタイプとしては成立している。特に、Web Search の `allowed_domains` をテナントごとに切り替える導線、オンメモリセッション、軽量ウィジェットは実装済みである。

一方で、販売先へ「スクリプトタグ」「ウィジェット JavaScript」「ウィジェット CSS」を配布する前提では、レートリミット、アクセスログ、README / 運用手順、ウィジェットの設定 API 連携が未整備である。また、SSE は `/v1/chat/stream` として実装されているため、仕様書の `/v1/chat/message` SSE 返却との API 契約差分は販売前に整理が必要である。
